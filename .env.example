# Required for production on Vercel (JSON file DB is not durable on serverless)
MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net/vit_event_portal

# JWT signing secret — change in production
JWT_SECRET=vit_secret_key_2026

# Optional: public API base (defaults to same-origin /api)
# NEXT_PUBLIC_API_URL=/api
